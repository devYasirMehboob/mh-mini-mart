import Icon from "../Icon";import {moneyKeys} from "./reportConfig";import {display,label} from "./reportFormat";
const preferred=['gross_sales','net_sales','total_discounts','discounts','cost_of_goods','gross_profit','total_expenses','estimated_net_profit','completed_sales','expense_count','total_expenses','stock_value','low_stock_count','out_of_stock_count','refunded_amount','average_sale_value','transaction_count','estimated_cost_impact','total_purchases','amount_paid','balance_due','total_supplier_balance','total_paid','total_returned','total_refunded'];
function ReportSummaryCards({summary={}}){const keys=[...new Set([...preferred,...Object.keys(summary)])].filter(key=>summary[key]!==undefined&&summary[key]!==null&&!Array.isArray(summary[key])&&typeof summary[key]!=='object').slice(0,8);if(keys.length===0)return null;return <section className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{keys.map((key,index)=><article key={key} className="premium-surface rounded-xl p-4"><div className="flex items-start justify-between gap-3"><div><p className="text-[9px] font-extrabold uppercase tracking-[0.12em] text-slate-400">{label(key)}</p><strong className="mt-2 block text-lg font-extrabold text-slate-950">{display(key,summary[key])}</strong></div><span className={`grid size-8 place-items-center rounded-lg ${index%4===0?'bg-blue-50 text-blue-600':index%4===1?'bg-emerald-50 text-emerald-600':index%4===2?'bg-amber-50 text-amber-600':'bg-violet-50 text-violet-600'}`}><Icon name={moneyKeys.has(key)?'cash':key.includes('stock')?'inventory':'reports'} className="size-4"/></span></div></article>)}</section>}
export default ReportSummaryCards;
