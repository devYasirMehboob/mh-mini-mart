import {useEffect} from "react";import Modal from "../Modal";import LoadingState from "../LoadingState";import {formatCurrency,formatDateTime} from "../../utils/calculateSaleTotals";import useSettings from "../../hooks/useSettings";
function ReceiptPreview({isOpen,receipt,isLoading,onClose,autoPrint=false}){
 const{settings}=useSettings();const options=receipt?.options||settings?.receipt||{};const shop=receipt?.shop||{};const logo=settings?.shop?.logo_url;
 useEffect(()=>{if(isOpen&&receipt&&autoPrint){const timer=setTimeout(()=>window.print(),350);return()=>clearTimeout(timer);}},[isOpen,receipt,autoPrint]);
 const totals=receipt?[["Subtotal",receipt.sale.subtotal],...(options.show_discount!==false?[["Discount",receipt.sale.discount_amount]]:[]),...(options.show_tax!==false&&options.tax_show_on_receipt!==false?[[(options.tax_name||"Tax"),receipt.sale.tax_amount]]:[]),["Grand total",receipt.sale.grand_total],["Received",receipt.sale.amount_received],...(options.show_change!==false?[["Change",receipt.sale.change_returned]]:[])]:[];
 return <Modal isOpen={isOpen} title="Receipt preview" description={`Saved ${options.paper_width||"80mm"} receipt data.`} onClose={onClose} size="sm">{isLoading?<LoadingState label="Loading receipt..."/>:receipt&&<div className="p-5"><article className="receipt-print-root mx-auto w-full bg-white p-4 font-mono text-[11px] text-black" style={{maxWidth:options.paper_width==="58mm"?"58mm":"80mm"}}><header className="text-center">{options.show_logo!==false&&logo&&<img src={logo} alt="" className="mx-auto mb-2 max-h-14 max-w-24 object-contain"/>}<h2 className="text-lg font-extrabold">{shop.name}</h2>{shop.address&&<p>{shop.address}</p>}{shop.phone&&<p>{shop.phone}</p>}{shop.registration_number&&<p>{shop.registration_number}</p>}<div className="my-3 border-t border-dashed border-black"/><p className="font-bold">{receipt.sale.invoice_number}</p><p>{formatDateTime(receipt.sale.created_at)}</p>{options.show_cashier!==false&&<p>Cashier: {receipt.sale.cashier_name} ({receipt.sale.cashier_role})</p>}{options.show_customer!==false&&receipt.sale.customer_name&&<p>Customer: {receipt.sale.customer_name}</p>}{options.show_customer!==false&&receipt.sale.customer_phone&&<p>Phone: {receipt.sale.customer_phone}</p>}</header>{receipt.sale.status!=="completed"&&<div className="my-3 border-2 border-black py-1 text-center text-base font-black uppercase">{receipt.sale.status}</div>}<div className="my-3 border-t border-dashed border-black"/><table className="w-full"><thead><tr><th className="pb-1 text-left">Item</th><th className="pb-1 text-right">Qty</th><th className="pb-1 text-right">Total</th></tr></thead><tbody>{receipt.sale.items.map(item=><tr key={item.id}><td className="py-1 pr-2">{item.product_name}<small className="block">{formatCurrency(item.unit_price)} each</small></td><td className="py-1 text-right align-top">{Number(item.quantity)}</td><td className="py-1 text-right align-top">{formatCurrency(item.line_total)}</td></tr>)}</tbody></table><div className="my-3 border-t border-dashed border-black"/><dl className="space-y-1">{totals.map(([label,value])=><div key={label} className={`flex justify-between ${label==="Grand total"?"text-sm font-black":""}`}><dt>{label}</dt><dd>{formatCurrency(value)}</dd></div>)}</dl>{options.show_payment_method!==false&&<p className="mt-2 capitalize">Payment: {receipt.sale.payment_method.replaceAll("_"," ")}</p>}<div className="my-3 border-t border-dashed border-black"/><footer className="space-y-1 text-center"><p>{shop.footer}</p><p>{shop.return_policy}</p></footer></article><div className="no-print mt-5 flex justify-end gap-2"><button type="button" onClick={onClose} className="rounded-lg border border-slate-200 px-4 py-2.5 text-xs font-bold text-slate-600">Close</button><button type="button" onClick={()=>window.print()} className="rounded-lg bg-blue-600 px-4 py-2.5 text-xs font-bold text-white">Print / Reprint</button></div></div>}</Modal>;
}export default ReceiptPreview;
