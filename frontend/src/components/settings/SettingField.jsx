function SettingField({field,value,error,onChange,disabled}){
 const[key,label,type,options]=field;const id=`setting-${key}`;if(type==="toggle")return <label htmlFor={id} className="flex min-h-16 cursor-pointer items-center justify-between gap-4 rounded-xl border border-slate-200 bg-slate-50/60 px-4"><span><strong className="block text-xs text-slate-800">{label}</strong>{error&&<small className="mt-1 block text-red-600">{error}</small>}</span><input id={id} type="checkbox" checked={Boolean(value)} disabled={disabled} onChange={e=>onChange(key,e.target.checked)} className="size-5 rounded border-slate-300 text-blue-600 focus:ring-blue-500"/></label>;
 const base="min-h-11 w-full rounded-xl border bg-white px-3 text-sm text-slate-800 outline-none transition focus:border-blue-400 focus:ring-4 focus:ring-blue-50";
 return <label htmlFor={id} className="block"><span className="mb-2 block text-xs font-bold text-slate-700">{label}</span>{type==="textarea"?<textarea id={id} rows="3" value={value??""} disabled={disabled} onChange={e=>onChange(key,e.target.value)} className={`${base} py-3 ${error?"border-red-300":"border-slate-200"}`}/>:type==="select"?<select id={id} value={value??""} disabled={disabled} onChange={e=>onChange(key,e.target.value)} className={`${base} ${error?"border-red-300":"border-slate-200"}`}>{options.map(([v,l])=><option key={v} value={v}>{l}</option>)}</select>:<input id={id} type={type} step={type==="number"?"any":undefined} value={value??""} disabled={disabled} onChange={e=>onChange(key,e.target.value)} className={`${base} ${error?"border-red-300":"border-slate-200"}`}/>} {error&&<small className="mt-1.5 block text-[11px] font-medium text-red-600">{error}</small>}</label>;
}export default SettingField;
