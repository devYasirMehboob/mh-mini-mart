import SettingField from "./SettingField";
function SettingsSectionForm({section,values,errors,onChange,disabled}){
 const featureOff=values?.enabled===false;
 return <section className="premium-surface rounded-xl p-5 sm:p-6"><header className="border-b border-slate-100 pb-5"><div className="flex items-center gap-2"><h3 className="text-lg font-extrabold tracking-tight text-slate-950">{section.label}</h3>{section.private&&<span className="rounded-full bg-amber-50 px-2 py-1 text-[9px] font-extrabold uppercase tracking-wider text-amber-700">Admin private</span>}</div><p className="mt-1 text-xs text-slate-500">{section.description}</p></header><div className="mt-6 grid gap-5 md:grid-cols-2">{section.fields.map(field=><SettingField key={field[0]} field={field} value={values?.[field[0]]} error={errors[`${section.key}.${field[0]}`]?.[0]} onChange={onChange} disabled={disabled||(featureOff&&field[0]!=="enabled")}/>)}</div>{section.key==="printer"&&values?.printing_method==="qz"&&<p className="mt-5 rounded-xl border border-amber-200 bg-amber-50 p-3 text-xs text-amber-800">QZ Tray is not installed by this project. Browser printing remains the working fallback; no test success will be simulated.</p>}{section.key==="backups"&&<p className="mt-5 rounded-xl border border-blue-100 bg-blue-50 p-3 text-xs text-blue-800">These preferences are stored for the Backups module. This page does not run or schedule backups.</p>}</section>;
}export default SettingsSectionForm;
